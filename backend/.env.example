# CTCDMS backend environment
# Copy to .env and set values

# Django
DJANGO_SECRET_KEY=your-secret-key
DJANGO_DEBUG=False
ALLOWED_HOSTS=localhost,127.0.0.1,your-domain.com

# PostgreSQL (when using docker-compose or production)
POSTGRES_DB=ctcdms
POSTGRES_USER=ctcdms
POSTGRES_PASSWORD=secure-password
POSTGRES_HOST=db
POSTGRES_PORT=5432

# CORS (production)
# CORS_ALLOWED_ORIGINS=https://your-frontend.com
